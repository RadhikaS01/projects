class ExampleService {
    constructor() {
        // Initialize any required properties or dependencies here
    }

    exampleMethod() {
        // Business logic for the example service
        return "This is an example method.";
    }

    anotherMethod(param) {
        // Another method that uses the parameter
        return `Received parameter: ${param}`;
    }
}

module.exports = ExampleService;